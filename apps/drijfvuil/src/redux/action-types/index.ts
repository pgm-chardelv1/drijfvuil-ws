export enum UserPositionActionType {
  SET_USER_POSTION = 'setUserPosition',
  RESET_USER_POSTION = 'resetUserPosition',
}

export enum ReportsActionType {
  SET_REPORTS = 'setReports',
  RESET_REPORTS = 'resetReports',
}

export enum CurrentReportActionType {
  SET_CURRENT_REPORT = 'setCurrentReport',
  RESET_CURRENT_REPORT = 'resetCurrentReport',
}
